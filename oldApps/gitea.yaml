compose:
  image: gitea/gitea
  volumes:
    - gitea:/data
    - /etc/timezone:/etc/timezone:ro
    - /etc/localtime:/etc/localtime:ro
  environment:
    - USER_UID=1000
    - USER_GID=1000

config:
  service:
    gitea: -3000

name: Gitea