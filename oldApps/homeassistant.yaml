compose:
  image: lscr.io/linuxserver/homeassistant
  network_mode: host
  environment:
    - PUID=1000
    - PGID=1000
    - TZ=Europe/London
  volumes:
    - homeassistant:/config

config:
  service:
    homeassistant: 8123